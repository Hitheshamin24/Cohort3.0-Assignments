// Placeholder — Hero animation functions will be built in Stage 5
// This file will contain gsap.timeline(), from(), stagger, and ease logic

export function heroEntrance(tl, refs) {
  // Will be implemented in Stage 5
}
