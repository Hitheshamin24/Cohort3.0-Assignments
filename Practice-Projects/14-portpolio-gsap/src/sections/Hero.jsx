// Hero section — static layout built in Stage 4, animated in Stage 5
const Hero = () => (
  <section id="hero" className="min-h-screen flex items-center justify-center">
    <p className="text-white font-mono text-sm">Hero — Stage 4</p>
  </section>
);
export default Hero;
